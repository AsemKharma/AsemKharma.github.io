# Images

Put images that accompany news posts here. Reference them from `index.html` like:

```html
<div class="news-post-media">
  <img src="images/your-image.jpg" alt="">
</div>
```

Replacing the emoji placeholder inside the news template will make the post show a real image at 16:9 aspect ratio.
